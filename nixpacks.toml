[phases.setup]
nixPkgs = ["nodejs-20_x"]

[phases.build]
cmds = ["npm install --include=dev", "npm run build", "npm run deploy-commands"]

[start]
cmd = "node --max-old-space-size=1024 dist/index.js"

[variables]
NODE_ENV = "production"
